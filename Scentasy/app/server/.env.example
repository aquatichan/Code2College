# Copy this file to .env and fill in your real key.
#   cp .env.example .env
#
# .env is gitignored - never commit your real key.

FRAGELLA_API_KEY=your_fragella_api_key_here

# Optional. Defaults to 8080, which is what the client dev proxy expects.
# PORT=8080
