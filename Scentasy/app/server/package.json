{
  "name": "scentasy-server",
  "version": "1.0.0",
  "description": "Server for Scentasy app",
  "main": "index.js",
  "private": true,
  "scripts": {
    "start": "node --env-file-if-exists=.env index.js",
    "devStart": "nodemon --env-file-if-exists=.env index.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
